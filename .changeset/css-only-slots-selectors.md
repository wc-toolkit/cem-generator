---
"@wc-toolkit/cem-generator": patch
---

Detect CSS-only custom elements referenced through selector lists, `:is()`, `:where()`, and `@scope` roots, and emit their documented and discovered slots.
